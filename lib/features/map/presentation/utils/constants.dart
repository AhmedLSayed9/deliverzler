import 'package:google_maps_flutter/google_maps_flutter.dart';

///Set defaultLatLng to your desired country [LAT_LNG], It's used if couldn't get user's location.
const LatLng defaultLatLng = LatLng(30.033333, 31.233334);
const double defaultMapZoom = 17.0;
const double defaultCircleRadius = 100.0;
const double defaultBoundsPadding = 60.0;
const double targetArriveDistance = 200.0; //in meters
